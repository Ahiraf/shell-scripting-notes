#! /bin/bash  
# Here First two characters are called 'shebang' and it tells the system which interpreter to use to execute the script. In this case, it specifies that the script should be run using the Bash shell.
# After shebang the path of the interpreter is given which we found by using 'which' command in terminal.

echo "Hello World"
